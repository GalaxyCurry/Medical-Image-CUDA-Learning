///////////////////构造函数（默认构造、带参构造、重载）


/* 1. 构造函数的本质
构造函数是类的特殊成员函数，用于对象创建时的初始化（如给成员变量赋初始值），特点：
无返回值（连void都不能写）；
函数名与类名完全相同；
对象创建时自动调用（无需手动调用）；
可重载（多个构造函数，参数列表不同）。 */






//2. 三种常见构造函数

1.默认构造函数
定义：无参数的构造函数（或所有参数都有默认值的构造函数）
两种形式：
// 形式1：无参构造
Person() { name = "未知"; age = 0; }

// 形式2：全默认参数构造（也属于默认构造）
Person(string n = "未知", int a = 0) { name = n; age = a; }

编译器自动生成：若类中没有定义任何构造函数，编译器会自动生成一个 “空体默认构造函数”（仅分配内存，不初始化内置类型成员）。


2.带参构造函数
定义：有参数的构造函数，用于创建对象时直接初始化成员变量。
作用：避免对象创建后再手动赋值，提高效率。


3.构造函数重载
定义：同一类中定义多个构造函数，参数列表（个数 / 类型 / 顺序）不同。
原则：重载函数必须有不同的参数特征，编译器根据实参匹配对应的构造函数。







#include <iostream>
#include <string>
using namespace std;

class Person {
private:
    string name;
    int age;

public:
    // 1. 默认构造函数（无参）
    Person() {
        cout << "默认构造函数调用" << endl;
        name = "未知";
        age = 0;
    }

    // 2. 带参构造函数
    Person(string n, int a) {
        cout << "带参构造函数调用" << endl;
        name = n;
        age = a;
    }

    // 3. 构造函数重载（全默认参数，也属于默认构造）
    Person(string n) {
        cout << "重载构造函数（单参数）调用" << endl;
        name = n;
        age = 18;  // 默认年龄18
    }

    void showInfo() {
        cout << "姓名：" << name << "，年龄：" << age << endl;
    }
};

int main() {
    Person p1;          // 调用默认构造（无参）
    p1.showInfo();      // 输出：姓名：未知，年龄：0

    Person p2("李四", 25);  // 调用带参构造
    p2.showInfo();      // 输出：姓名：李四，年龄：25

    Person p3("王五");   // 调用重载构造（单参数）
    p3.showInfo();      // 输出：姓名：王五，年龄：18

    // 注意：不能同时定义无参构造和全默认参数构造（二义性）
    // Person p4("赵六", 30);  // 若有Person(string n="a", int a=0)，则与带参构造冲突
    return 0;
}





















三、常见问题与解决方案



问题 1：自定义带参构造后，无法无参创建对象

class Person {
public:
    Person(string n, int a) { ... }  // 仅定义带参构造
};
Person p;  // 编译错误：无默认构造函数


原因：若类中定义了任何构造函数，编译器不再自动生成默认构造。
解决方案：手动定义默认构造函数（无参或全默认参数）。



问题 2：构造函数重载导致二义性

Person() { ... }
Person(string n = "未知") { ... }  // 全默认参数
Person p;  // 编译错误：无法区分调用哪个默认构造

原因：两个构造函数都可无参调用，编译器无法匹配。
解决方案：确保默认构造函数唯一（要么无参，要么全默认参数，二选一）



问题 3：内置类型成员变量未初始化

class Test {
public:
    int x;  // 内置类型
    Test() {}  // 空体默认构造
};
Test t;
cout << t.x;  // 输出随机值（未初始化）

原因：编译器生成的默认构造或自定义空体构造，不会初始化内置类型（int、double、指针等）。
解决方案：在构造函数中显式初始化内置类型成员，或使用类内初始化（C++11+）：int x = 0;。



问题 4：误用 “默认构造函数” 的调用语法

错误示例：Person p();（意图调用无参构造）
原因：编译器会解析为 “返回 Person 类型的无参函数声明”，而非对象创建。
解决方案：无参创建对象直接写Person p;（不加括号）。